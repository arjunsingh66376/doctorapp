import 'package:flutter/material.dart';

class Bookedpage extends StatelessWidget {
  const Bookedpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('booked page ')));
  }
}
